{
  "name": "Docker Registry",
  "version": "1.0",
  "slug": "docker-registry",
  "description": "Run Docker Registry within Home Assistant.",
  "startup": "services",
  "arch": ["armv7", "aarch64", "amd64"],
  "boot": "auto",
  "map": ["config", "ssl"],
  "options": {},
  "schema": {}
}
